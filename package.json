{
  "name": "task-management-assessment",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c green,blue \"npm run start:dev -w apps/api\" \"npm run dev -w apps/web\"",
    "build": "npm run build -w apps/api && npm run build -w apps/web",
    "lint": "npm run lint -w apps/api && npm run lint -w apps/web",
    "test": "npm run test -w apps/api && npm run test -w apps/web",
    "format": "prettier --write \"apps/**/*.{ts,tsx,css,md,json}\" \"docs/**/*.md\" README.md"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "next": "^16.3.0",
    "prettier": "^3.6.2"
  }
}
